import Layout from "@/components/layout";
import type { NextPage } from "next";

const Page: NextPage = () => {
  return (
      <Layout prevPage title="내 정보">
        <div>
          내화면이요
        </div>
      </Layout>
    );
};

export default Page;
